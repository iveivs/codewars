function findUniq(arr) {
    // let newArr = arr.filter( function(elem, i, arr) {
    //     return elem !== arr[i + 1]
    // } )
    // console.log(newArr);
    // return newArr
    let i = 0
    let oftenVar = 0
    while(i < arr.length){
        console.log(arr[i])
        if(arr)
        i++;
    }
}
console.log(findUniq([ 1, 1, 1, 2, 1, 1 ]));

